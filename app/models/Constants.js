var Constants = {
    playerName: "Hoijof",
    playerSurname: "Golpeo"
};


Constants.getShortName = function (name) {
    "use strict";
    var result = "";

    switch (name) {

    }

    return result !== "" ? result : undefined
};